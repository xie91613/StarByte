# StarByte 计算机协会管理系统 — 总体架构设计

> **版本**: v1.0
> **日期**: 2026-08-24
> **状态**: 一期设计

---

## 1. 项目概述

StarByte 是一个面向高校计算机协会的全功能管理系统，涵盖入会申请、人员档案、面试管理、任务流转、会议投票、合同管理、纪律处分、IT 实习、财务管理等协会日常运营的方方面面。

系统采用模块化设计，底层 OA 流程引擎为核心，支持拖拽式流程配置，以适应不同届管理层的差异化需求。

---

## 2. 技术栈

| 层级 | 技术选型 | 版本 | 说明 |
|------|---------|------|------|
| **前端框架** | React | 18.x | UI 框架 |
| **前端语言** | TypeScript | 5.x | 类型安全 |
| **构建工具** | Vite | 5.x | 开发构建 |
| **状态管理** | Redux Toolkit | 2.x | 全局状态管理 |
| **UI 组件库** | Ant Design | 5.x | 企业级 UI 组件库 |
| **流程设计器** | React Flow | 11.x | 可拖拽流程图 |
| **图表库** | ECharts | 5.x | 数据可视化 |
| **路由** | React Router | 6.x | 前端路由 |
| **HTTP 客户端** | Axios | 1.x | API 请求 |
| **后端语言** | Go | 1.22 | 后端开发语言 |
| **Web 框架** | Gin | 1.9.x | HTTP 框架 |
| **ORM** | GORM | 1.25.x | 数据库 ORM |
| **数据库** | PostgreSQL | 16.x | 关系型数据库 |
| **数据库迁移** | golang-migrate | 4.x | Schema 版本管理 |
| **缓存/消息** | Redis | 7.x | 缓存 + WebSocket pub/sub |
| **文件存储** | MinIO | latest | 对象存储（兼容 S3） |
| **鉴权** | JWT + Refresh Token | - | 无状态鉴权 |
| **WebSocket** | Gorilla WebSocket | 1.x | 实时通信 |
| **容器化** | Docker Compose | - | 本地开发 & 部署 |
| **API 风格** | RESTful | - | `/api/v1/` 前缀 |
| **Git 工作流** | GitHub Flow | - | Squash Merge |

---

## 3. 系统架构

### 3.1 整体架构图

```
┌─────────────────────────────────────────────────────────────────────┐
│                        前端层 (React SPA)                            │
│  ┌─────────┬──────────┬──────────┬──────────┬──────────┬─────────┐ │
│  │  登录页 │  工作台  │ 业务模块 │ 流程设计 │ 系统管理 │ 统计报表│ │
│  └─────────┴──────────┴──────────┴──────────┴──────────┴─────────┘ │
│         Redux Toolkit / Ant Design / ECharts / React Flow          │
└───────────────────────────────┬─────────────────────────────────────┘
                                │ RESTful API + WebSocket
┌───────────────────────────────┴─────────────────────────────────────┐
│                        网关层 (Nginx)                                │
│              静态资源托管 / API 反向代理 / TLS 终止                   │
└───────────────────────────────┬─────────────────────────────────────┘
                                │
┌───────────────────────────────┴─────────────────────────────────────┐
│                        应用层 (Gin)                                  │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │                    中间件层 (Middleware)                      │   │
│  │   CORS / JWT鉴权 / 审计日志 / 限流 / 恐慌恢复 / 请求ID        │   │
│  └──────────────────────────────────────────────────────────────┘   │
│                                                                    │
│  ┌──────┬───────┬───────┬───────┬───────┬───────┬───────┬──────┐   │
│  │ 用户 │ 会员  │ 面试  │ 会议  │ 任务  │ 实习  │ 通知  │ 统计 │   │
│  │ 模块 │ 模块  │ 模块  │ 模块  │ 模块  │ 模块  │ 模块  │ 模块 │   │
│  └──┬───┴───┬───┴───┬───┴───┬───┴───┬───┴───┬───┴───┬───┴────┘   │
│     │ 财务 / 处分 / 合同（一期预留产品页，写主库）                 │
│     │       │       │       │       │       │       │            │
│  ┌──┴───────┴───────┴───────┴───────┴───────┴───────┴────┐       │
│  │              流程引擎 (Workflow Engine)               │       │
│  │   节点插件化 / 表达式引擎 / 事件驱动 / 任务调度        │       │
│  └───────────────────────┬──────────────────────────────┘       │
│                          │                                       │
│  ┌───────────────────────┴──────────────────────────────┐       │
│  │                    公共服务层 (Shared)                │       │
│  │  RBAC权限 / 配置中心 / 文件存储 / 审计 / WebSocket     │       │
│  └──────────────────────────────────────────────────────┘       │
└───┬──────────────────────────┬──────────────────────────┬───────┘
    │                          │                          │
┌───▼─────┐              ┌────▼─────┐              ┌─────▼────┐
│PostgreSQL│              │   Redis   │              │   MinIO   │
│  主数据  │              │ 缓存/消息  │              │ 文件存储   │
└─────────┘              └───────────┘              └──────────┘
```

### 3.2 架构原则

1. **模块化**：每个业务模块自包含，通过明确定义的接口交互
2. **插件化**：流程引擎、通知渠道、权限策略等均采用插件/策略模式，便于扩展
3. **配置化**：避免硬编码，所有业务配置（权限开关、流程定义、通知模板等）均可动态配置
4. **事件驱动**：核心业务动作触发领域事件，通知、审计、统计等通过订阅事件解耦
5. **可扩展性**：二期功能预留接口，数据模型预留扩展字段

---

## 4. 项目结构

### 4.1 Monorepo 目录结构

```
StarByte/
├── backend/                    # Go 后端
│   ├── cmd/
│   │   └── server/             # 服务入口
│   │       └── main.go
│   ├── internal/               # 内部业务模块（不对外暴露）
│   │   ├── user/               # 用户模块
│   │   ├── auth/               # 认证模块
│   │   ├── rbac/               # RBAC 权限模块
│   │   ├── organization/       # 组织架构模块
│   │   ├── member/             # 会员/入会申请模块
│   │   ├── interview/          # 面试管理模块
│   │   ├── meeting/            # 会议管理模块
│   │   ├── task/               # 任务流转模块
│   │   ├── workflow/           # 流程引擎模块（核心）
│   │   ├── notification/       # 消息通知模块
│   │   ├── internship/         # IT 实习管理模块
│   │   ├── audit/              # 审计日志模块
│   │   ├── stats/              # 数据统计模块
│   │   └── file/               # 文件管理模块
│   ├── pkg/                    # 公共包（可被外部引用）
│   │   ├── config/             # 配置管理
│   │   ├── database/           # 数据库连接
│   │   ├── redis/              # Redis 客户端
│   │   ├── minio/              # MinIO 客户端
│   │   ├── middleware/         # Gin 中间件
│   │   ├── response/           # 统一响应格式
│   │   ├── logger/             # 日志
│   │   ├── utils/              # 工具函数
│   │   ├── ws/                 # WebSocket 管理
│   │   └── events/             # 事件总线
│   ├── migrations/             # 数据库迁移文件
│   │   ├── 000001_init_schema.up.sql
│   │   └── ...
│   ├── configs/                # 配置文件
│   │   └── config.yaml
│   ├── Dockerfile
│   ├── go.mod
│   └── go.sum
├── frontend/                   # React 前端
│   ├── src/
│   │   ├── api/                # API 请求封装
│   │   ├── assets/             # 静态资源
│   │   ├── components/         # 公共组件
│   │   ├── layouts/            # 布局组件
│   │   ├── pages/              # 页面（按业务模块组织）
│   │   │   ├── login/
│   │   │   ├── dashboard/
│   │   │   ├── user/
│   │   │   ├── member/
│   │   │   ├── interview/
│   │   │   ├── meeting/
│   │   │   ├── task/
│   │   │   ├── workflow/
│   │   │   ├── internship/
│   │   │   ├── stats/
│   │   │   └── system/
│   │   ├── store/              # Redux Store
│   │   │   ├── index.ts
│   │   │   └── slices/         # 按模块拆分 slice
│   │   ├── hooks/              # 自定义 Hooks
│   │   ├── utils/              # 工具函数
│   │   ├── types/              # TypeScript 类型定义
│   │   ├── router/             # 路由配置
│   │   ├── styles/             # 全局样式
│   │   └── App.tsx
│   ├── public/
│   ├── Dockerfile
│   ├── package.json
│   ├── tsconfig.json
│   └── vite.config.ts
├── docs/                       # 项目文档
│   ├── specs/                  # 设计文档
│   │   ├── 00-overall-architecture.md
│   │   ├── 01-workflow-engine.md
│   │   ├── 02-rbac-system.md
│   │   └── ...
│   ├── dev-guide/              # 开发规范
│   │   ├── backend.md
│   │   ├── frontend.md
│   │   ├── git-workflow.md
│   │   ├── pr-specification.md
│   │   └── ai-development-prompt.md
│   └── api/                    # API 文档
├── deploy/                     # 部署配置
│   └── docker-compose.yml
├── .github/                    # GitHub 配置
│   ├── workflows/              # CI/CD 工作流
│   ├── pull_request_template.md
│   └── ISSUE_TEMPLATE/
├── .gitignore
├── .editorconfig
└── README.md
```

### 4.2 后端模块分层规范

每个业务模块遵循统一的四层架构：

```
module/
├── handler/        # 接口层：处理 HTTP 请求，参数校验，调用 service
├── service/        # 业务逻辑层：核心业务逻辑，事务控制，调用 repo
├── repo/           # 数据访问层：数据库 CRUD，GORM 操作
├── model/          # 数据模型层：数据库表结构定义（GORM Model）
└── dto/            # 数据传输对象：请求/响应结构体定义
```

**依赖方向**：handler → service → repo → model
**禁止反向依赖**，不允许 repo 层调用 service 层。

---

## 5. 核心模块设计概要

### 5.1 用户系统

- **主键策略**：UUID v4，不使用自增 ID
- **多身份关联**：一个用户可关联多个学号（本科/研究生），通过 `user_identities` 表管理
- **登录方式**：账号密码（一期）+ 校园网 CAS（`authserver.smbu.edu.cn`，漏测按访问 IP 回调）；预留微信/OAuth
- **密码加密**：bcrypt
- **Token 机制**：Access Token（15分钟） + Refresh Token（7天），支持黑名单

### 5.2 RBAC 权限系统

- **模型**：用户 → 角色 → 权限，支持多角色
- **权限粒度**：API 级 + 数据级（部门数据权限）
- **配置化**：所有权限和角色均可在后台管理页面配置，不硬编码
- **权限开关**：每个功能模块都有独立的权限开关，由社长/管理员控制

### 5.3 流程引擎（核心）

- **节点插件化**：每个节点类型实现统一的 `NodeHandler` 接口，新增节点类型无需改核心代码
- **节点类型**：
  - 开始节点、结束节点
  - 审批节点（单人审批 / 多人会签 / 多人或签 / 比例通过）
  - 条件分支节点（表达式判断）
  - 并行网关（多条路径同时执行）
  - 任务节点（自动执行预设动作，如发通知、更新数据）
- **表达式引擎**：基于 govaluate，支持条件判断
- **事件驱动**：流程启动、任务创建、审批通过、流程结束等均触发领域事件
- **版本管理**：流程定义支持多版本，已运行的实例使用启动时的版本

### 5.4 通知系统

- **策略模式**：多种通知渠道实现统一的 `Notifier` 接口
- **渠道类型**：
  - 站内消息（数据库存储，必选）
  - WebSocket 实时推送（必选）
  - 邮件通知（SMTP，一期）
  - 预留：微信公众号、企业微信、短信（二期）
- **通知模板**：支持模板配置，变量替换
- **已读状态**：站内消息支持已读/未读

### 5.5 审计日志

- **记录范围**：
  - 所有写操作（POST / PUT / DELETE）
  - 登录 / 登出
- **记录内容**：操作人ID、操作时间、IP地址、User-Agent、请求路径、请求方法、请求参数、响应状态码、响应结果（可选）、耗时
- **实现方式**：Gin 中间件自动拦截，异步写入数据库
- **查询**：支持按操作人、操作类型、时间范围、IP 等维度检索

---

## 6. 数据模型概要

### 6.1 用户与权限

| 表名 | 说明 |
|------|------|
| `users` | 用户主表（UUID 主键） |
| `user_identities` | 用户身份关联表（学号、工号等） |
| `roles` | 角色表 |
| `permissions` | 权限表 |
| `role_permissions` | 角色-权限关联表 |
| `user_roles` | 用户-角色关联表 |

### 6.2 组织架构

| 表名 | 说明 |
|------|------|
| `departments` | 部门表 |
| `positions` | 职务表 |
| `position_weights` | 职务权重表（用于加权投票） |

### 6.3 流程引擎

| 表名 | 说明 |
|------|------|
| `flow_definitions` | 流程定义表 |
| `flow_definition_versions` | 流程定义版本表 |
| `flow_instances` | 流程实例表 |
| `flow_tasks` | 流程任务表 |
| `flow_task_candidates` | 任务候选人表 |
| `flow_histories` | 流程历史记录表 |
| `flow_variables` | 流程变量表 |

### 6.4 业务模块

| 表名 | 说明 | 所属模块 |
|------|------|---------|
| `member_applications` | 入会申请表 | 会员模块 |
| `members` | 会员档案表 | 会员模块 |
| `interviews` | 面试安排表 | 面试模块 |
| `interview_scores` | 面试评分表 | 面试模块 |
| `meetings` | 会议表 | 会议模块 |
| `meeting_attendees` | 会议参会人员表 | 会议模块 |
| `votes` | 投票表 | 会议模块 |
| `vote_options` | 投票选项表 | 会议模块 |
| `tasks` | 任务表 | 任务模块 |
| `task_comments` | 任务评论表 | 任务模块 |
| `internships` | 实习记录表 | 实习模块 |
| `notifications` | 通知消息表 | 通知模块 |
| `audit_logs` | 审计日志表 | 审计模块 |
| `files` | 文件记录表 | 文件模块 |
| `configs` | 系统配置表 | 配置中心 |

---

## 7. API 设计规范

### 7.1 统一响应格式

```json
{
  "code": 0,
  "message": "success",
  "data": {},
  "request_id": "uuid-string",
  "timestamp": 1234567890
}
```

- `code`: 0 表示成功，非 0 表示错误
- `message`: 错误描述，成功时为 "success"
- `data`: 响应数据，失败时可为 null
- `request_id`: 请求唯一标识，用于排查问题
- `timestamp`: 响应时间戳

### 7.2 错误码规范

| 错误码范围 | 说明 |
|-----------|------|
| 0 | 成功 |
| 1000-1999 | 通用错误（参数错误、未授权、禁止访问、不存在等） |
| 2000-2999 | 用户模块错误 |
| 3000-3999 | 权限模块错误 |
| 4000-4999 | 流程引擎错误 |
| 5000-5999 | 业务模块错误（按子模块再细分） |

### 7.3 RESTful API 命名规范

- 使用复数名词：`/api/v1/users` 而非 `/api/v1/user`
- 使用 HTTP 动词表达操作：GET（查询）、POST（创建）、PUT（更新）、DELETE（删除）
- 层级关系用路径：`/api/v1/departments/{deptId}/members`
- 过滤/排序/分页用 Query 参数

---

## 8. 配置管理

- **配置文件**：YAML 格式，支持环境变量覆盖
- **配置中心**：数据库 `configs` 表存储动态配置（权限开关、流程配置等）
- **环境区分**：dev / test / prod 三套配置

---

## 9. 非功能性需求

### 9.1 安全
- 密码 bcrypt 加密存储
- JWT 签名验证 + Refresh Token 机制
- SQL 注入防护（GORM 参数化查询）
- XSS 防护（前端转义 + 后端校验）
- CSRF 防护（JWT 机制天然免疫）
- 接口限流（Redis + 令牌桶）

### 9.2 性能
- 热点数据 Redis 缓存
- 数据库索引优化
- 分页查询
- 异步处理（审计日志、通知发送等）

### 9.3 可扩展性
- 模块化架构，新增模块不影响现有模块
- 插件化设计，流程节点、通知渠道可扩展
- 配置化，业务规则不硬编码

---

## 10. 一期 / 二期功能划分

### 一期功能
- ✅ 用户系统（注册、登录、JWT 鉴权）
- ✅ RBAC 权限系统 + 组织架构
- ✅ 可拖拽流程引擎（设计器 + 引擎 + API）
- ✅ 入会申请（会员/干事）
- ✅ 人员档案管理
- ✅ 面试管理（接入流程引擎）
- ✅ 会议管理 + 等权匿名投票
- ✅ 任务流转
- ✅ IT 实习管理
- ✅ 消息通知（站内 + WebSocket + 邮件）
- ✅ 系统审计日志
- ✅ 数据统计与可视化报表
- ✅ 文件管理（MinIO）
- ✅ 财务管理（记录 CRUD、分类、汇总；导出预留）
- ✅ 纪律处分（登记、审批、撤销、申诉、通知；可选挂接流程 `discipline_approve`）
- ✅ 合同管理（模板、附件、到期扫描）
- ✅ 前端 i18n（zh-CN / en-US）与亮/暗/跟随系统主题

### 二期功能
- 🔲 微信扫码登录 / 第三方 OAuth / 外网域名跳转
- 🔲 入会申请接入流程引擎
- 🔲 任务流转接入流程引擎
- 🔲 更多通知渠道（微信公众号、企业微信、短信）
- 🔲 财务导出引擎与报销审批完善
- 🔲 移动端适配 / 小程序

---

## 11. 模块依赖（一期）

```
auth / user ──► rbac ──► 各业务模块（permission + data_scope）
member ──► interview（干事申请可启动流程）
interview / meeting / task / discipline ──► notification
discipline ──► workflow（可选 key=discipline_approve；无定义则模块内审批）
contract ──► file（附件 file_id）
contract ──► scheduler（handler_key=contract_expiry）
finance ──► export（一期 /finance/export 返回 1501 预留）
frontend ──► antd ConfigProvider + i18next（壳层/新模块）
```

财务、处分、合同写本系统主库。校园网 CAS（`authserver.smbu.edu.cn`）已接入 auth；学校 HAP / 企微属二期。

